# Local dev deliberately does NOT set VITE_API_URL.
#
# vite.config.ts always enables @vitejs/plugin-basic-ssl, so `npm run dev`
# serves the frontend over https://localhost:<port> (self-signed cert) even
# in dev — this was required earlier so getUserMedia()/mic access works.
#
# If VITE_API_URL were set to an http:// address (e.g. http://localhost:4000),
# every fetch/WebSocket call would go directly to that plain-HTTP origin from
# an https:// page. Chrome treats that as mixed content: it silently
# auto-upgrades the request to https://localhost:4000, which fails because
# the backend has no TLS listener on that port — the exact bug already fixed
# once for /uploads (see project memory). Requests would look like they
# "never happen" (no network entry, just a console warning).
#
# Instead, vite.config.ts's dev proxy (/api, /socket, /uploads -> backend on
# http://localhost:4000) makes the frontend and backend look same-origin, so
# src/config.ts's API_BASE_URL stays "" and everything resolves relative to
# whatever origin the page is actually loaded from. Leave the line below
# commented out for local dev.

# VITE_API_URL=http://localhost:4000

# Production TURN relay for voice channels (see useVoiceChannel.ts's
# resolveIceServers()) — unset in dev/LAN, where STUN alone is enough since
# there's no symmetric NAT/restrictive firewall between peers to traverse.
#
# Option A — managed provider (Metered.ca, or a backend proxy in front of
# Xirsys) that hands out fresh short-lived credentials over HTTP:
# VITE_TURN_API_URL=https://your-app.metered.live/api/v1/turn/credentials?apiKey=your-api-key
#
# Option B — a single static TURN server (e.g. self-hosted coturn). All
# three must be set together or none are used.
# VITE_TURN_URL=turn:your-turn-server.com:3478
# VITE_TURN_USERNAME=your-turn-username
# VITE_TURN_PASSWORD=your-turn-password
