import Config

# Copy this file to dev.secret.exs (gitignored — see .gitignore) and fill
# in real values. Loaded automatically at the end of config/dev.exs if
# present; leave dev.secret.exs absent entirely if you don't need any of
# this — nothing in dev/test currently requires an env var or a secret
# to run (see PROJECT_ARCHITECTURE.md's note on this).
#
# Example — a managed TURN provider's server-side API key, for testing a
# future backend proxy in front of it locally (see
# PROJECT_ARCHITECTURE.md 4.4's note on why a provider like Xirsys needs
# a server-side-only secret rather than a VITE_* frontend env var):
#
# config :backend, :metered_api_key, "your-key-here"
