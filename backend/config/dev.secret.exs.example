import Config

# Copy this file to dev.secret.exs (gitignored — see .gitignore) and fill
# in real values. Loaded automatically at the end of config/dev.exs if
# present; leave dev.secret.exs absent entirely if you don't need any of
# this — nothing else in dev/test requires an env var or a secret to run
# (see PROJECT_ARCHITECTURE.md's note on this).

# Metered.ca TURN Server static username/credential pair (see
# Backend.Turn, BackendWeb.VoiceController, PROJECT_ARCHITECTURE.md
# 2.9) — dashboard-issued, long-lived (not a per-request API token),
# kept server-side so it never reaches the frontend. Absent means
# Backend.Turn.fetch_ice_servers/0 falls back to STUN-only and logs a
# warning, not an error — safe to leave commented out.
# config :backend, :turn_config, %{username: "your-username", credential: "your-credential"}
